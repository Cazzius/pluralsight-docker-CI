# The deployment section should be excluded until
# it is explicitly included in module 5
# simple update to test end to end

deployment:
  dockerhub:
    branch: master
    commands:
      - $DockerHubTrigger

test:
  override:    
    - mocha
